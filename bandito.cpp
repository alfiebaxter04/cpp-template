#include <bits/stdc++.h>

#define fastio ios::sync_with_stdio(false); cin.tie(nullptr);

using namespace std;
using ll = long long;

void solve() {
	int n, m, l, r;
	cin >> n >> m >> l >> r;

	cout << l << " " << l + m << "\n";
}

int32_t main(){
	fastio;
	int t;
	cin >> t;
	
	while (t--) solve();
}