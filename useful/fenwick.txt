
// (x & -x) gives lsb

struct BIT {
	int n;
	vector<int> tree;

	BIT(int n) : n(n), tree(n+1, 0) {}

	void update(int x, int val) { x++; while (x <= 100005) { tree[x] += val; x += (x & -x); } }
	int query(int x) { x++; int res = 0; while (x > 0) { res += tree[x]; x -= (x & -x); } return res; }
};